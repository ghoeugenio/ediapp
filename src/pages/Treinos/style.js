import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: '#212121',
        flex: 1,
    },
    textest: {
        textAlign: 'center',
        paddingTop: 50,
    }
});
